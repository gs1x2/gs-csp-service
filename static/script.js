document.addEventListener('DOMContentLoaded', () => {
    
    document.body.classList.add('loaded'); // Плавное появление страницы

    // Допустим, можно добавить анимацию при наведении на элементы или что-то еще
    // Здесь простая логика уже реализована через CSS :hover
    // Можно также добавить дополнительную анимацию, например, всплывающую подсказку
});
